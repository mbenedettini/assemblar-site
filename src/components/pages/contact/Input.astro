---
interface Props {
  label: string;
  name: string;
  placeholder: string;
}

const { label, name, placeholder } = Astro.props;
---

<div>
  <label class='mb-2 text-primary block text-sm' for={name}>{label}</label>
  <div
    class='flex items-center gap-2 px-4 py-3 rounded-lg border border-gray-blue-300/60 bg-gray-blue-900'
  >
    <slot name='icon' />

    <input
      class='text-sm bg-transparent placeholder:text-white w-full outline-none'
      type='text'
      {name}
      {placeholder}
    />
  </div>
</div>
